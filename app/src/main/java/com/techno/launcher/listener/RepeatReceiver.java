package com.techno.launcher.listener;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

import com.techno.launcher.global.LiveMosqueSingleTone;
import com.techno.launcher.screens.HomeActivity;

public class RepeatReceiver {
//        extends BroadcastReceiver {
//    @Override
//    public void onReceive(Context context, Intent intent) {
////        try {
////            if (intent.getAction().equalsIgnoreCase("ALARM")) {
////                ((HomeActivity) HomeActivity.getInstance()).implementLogics(LiveMosqueSingleTone.getInstance().getPrayerTimes());
////            }
////        } catch (Exception e) {
////            e.printStackTrace();
////        }
//
//    }
}

